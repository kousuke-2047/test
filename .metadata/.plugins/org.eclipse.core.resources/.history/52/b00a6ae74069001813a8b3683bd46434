package com.rensyu.action;

import com.opensymphony.xwork2.ActionSupport;
import java.util.Map;
import javax.websocket.Session;

import org.apache.struts2.interceptor.SessionAware;
import com.rensyu.dao.BuyItemCDAO;
import com.rensyu.dto.BuyItemCDTO;
import com.rensyu.dto.LoginDTO;



public class BuyItemCompleteAction extends ActionSupport implements SessionAware{

	BuyItemCDAO buyitemcdao = new BuyItemCDAO();
	BuyItemCDTO buyitemcdto = new BuyItemCDTO();
	LoginDTO logindto = new LoginDTO();


	public Map<String ,Object>session;

	public String execute(){
		String result;
		result =SUCCESS;

		buyitemcdao.BuyItemInfo( session.get("ItemName").toString(), session.get("total").toString(), session.get("count").toString(), session.get("userId").toString(), session.get("pay").toString());



		return result;
	}

	public void setSession(Map<String ,Object>session){
		this.session = session;;
	}

}
